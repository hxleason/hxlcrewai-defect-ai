# app/db/__init__.py
"""
Database package initialization.
编辑此文件属于常见正常工作；但如果因 IDE 自动保存而混入配置项，请立即删除。
"""

# 如果你的项目架构需要从包中统一导出，可取消注释下面两行
# from .database import engine, Base
# __all__ = ["engine", "Base"]

# 注意：永远不要在 Python 文件中硬编码密码、API Key 等密钥。
# 请使用 .env 文件 + load_dotenv() 或 pydantic Settings 管理配置。